---
metaTitle: 无障碍图标
metaDescription: 通过添加标签使图标可访问。
name: accessible-icon
---

# 无障碍图标

<Description>通过添加标签使图标可访问。</Description>

<Highlights
	features={[
		"通过将任何图标包装并提供有意义的标签，快速使其可访问。",
		"没有视觉差异，但被屏幕阅读器正确宣布。",
	]}
/>

## 安装

从命令行安装组件。

```bash
npm install @radix-ui/react-accessible-icon
```

## 结构

导入组件。

```jsx
import { AccessibleIcon } from "radix-ui";

export default () => <AccessibleIcon.Root />;
```

## API 参考

### 根部

包含要使其可访问的图标。

<PropsTable
	data={[
		{
			name: "label",
			required: true,
			type: "string",
			description: (
				<span>
					图标的可访问标签。此标签将在视觉上隐藏，但会被屏幕阅读器用户宣布，类似于 <Code>alt</Code> 文本针对 <Code>img</Code> 标签。
				</span>
			),
		},
	]}
/>

## 可访问性

大多数图标或图标系统没有内置的可访问性。例如，相同的视觉 **交叉** 图标实际上可能表示 **“关闭”** 或 **“删除”**。该组件允许您为应用程序中使用的图标赋予意义。

这是使用 [视觉隐藏](../utilities/visually-hidden) 构建的。