---
metaTitle: 滚动区域
metaDescription: 增强自定义跨浏览器样式的本地滚动功能。
name: scroll-area
---

# 滚动区域

<Description>
	增强自定义跨浏览器样式的本地滚动功能。
</Description>

<HeroContainer>
	<ScrollAreaDemo />
</HeroContainer>

<HeroCodeBlock folder="ScrollArea" />

<Highlights
	features={[
		"滚动条位于可滚动内容之上，不占用空间。",
		"滚动是原生的；没有通过 CSS 变换导致的底层位置移动。",
		"仅在与控件交互时调整指针行为，因此键盘控制不受影响。",
		"支持从右到左的方向。",
	]}
/>

## 安装

从命令行安装组件。

```bash
npm install @radix-ui/react-scroll-area
```

## 解剖结构

导入所有部分并组装在一起。

```jsx
import { ScrollArea } from "radix-ui";

export default () => (
	<ScrollArea.Root>
		<ScrollArea.Viewport />
		<ScrollArea.Scrollbar orientation="horizontal">
			<ScrollArea.Thumb />
		</ScrollArea.Scrollbar>
		<ScrollArea.Scrollbar orientation="vertical">
			<ScrollArea.Thumb />
		</ScrollArea.Scrollbar>
		<ScrollArea.Corner />
	</ScrollArea.Root>
);
```

## API 参考

### 根

包含滚动区域的所有部分。

<PropsTable
	data={[
		{
			name: "asChild",
			required: false,
			type: "boolean",
			default: "false",
			description: (
				<>
					将默认渲染的元素更改为作为子元素传递的元素，合并它们的属性和行为。
					<br />
					<br />
					有关更多详细信息，请查看我们的 <a href="../guides/composition">组合</a> 指南。
				</>
			),
		},
		{
			name: "type",
			type: '"auto" | "always" | "scroll" | "hover"',
			typeSimple: "enum",
			default: '"hover"',
			description: (
				<span>
					描述滚动条可见性的性质，类似于 MacOS 中的滚动条偏好设置如何控制原生滚动条的可见性。
					<br />
					<br />
					<Code>"auto"</Code> 意味着当内容在相应方向上溢出时，滚动条可见。
					<br />
					<Code>"always"</Code> 意味着滚动条始终可见，无论内容是否溢出。
					<br />
					<Code>"scroll"</Code> 意味着当用户在相应方向上滚动时，滚动条可见。
					<br />
					<Code>"hover"</Code> 意味着当用户在相应方向上滚动并且当用户悬停在滚动区域上时。
				</span>
			),
		},
		{
			name: "scrollHideDelay",
			type: "number",
			default: "600",
			description: (
				<span>
					如果 <Code>type</Code> 设置为 <Code>"scroll"</Code> 或{" "}
					<Code>"hover"</Code>，则此属性确定用户停止与滚动条交互后，滚动条隐藏的时间长度（以毫秒为单位）。
				</span>
			),
		},
		{
			name: "dir",
			required: false,
			type: '"ltr" | "rtl"',
			typeSimple: "enum",
			description: (
				<span>
					滚动区域的阅读方向。如果省略，则从 <Code>DirectionProvider</Code> 全局继承，或假定为从左到右的阅读模式。
				</span>
			),
		},
		{
			name: "nonce",
			required: false,
			type: "string",
			description: (
				<span>
					一个可选的 nonce 属性，传递给内联样式，用于使用严格规则增强安全性的 CSP 启用环境。
				</span>
			),
		},
	]}
/>

### 视口

滚动区域的视口区域。

<PropsTable
	data={[
		{
			name: "asChild",
			required: false,
			type: "boolean",
			default: "false",
			description: (
				<>
					将默认渲染的元素更改为作为子元素传递的元素，合并它们的属性和行为。
					<br />
					<br />
					有关更多详细信息，请查看我们的 <a href="../guides/composition">组合</a> 指南。
				</>
			),
		},
	]}
/>

### 滚动条

垂直滚动条。添加第二个 `Scrollbar` 并设置 `orientation` 属性以启用水平滚动。

<PropsTable
	data={[
		{
			name: "asChild",
			required: false,
			type: "boolean",
			default: "false",
			description: (
				<>
					将默认渲染的元素更改为作为子元素传递的元素，合并它们的属性和行为。
					<br />
					<br />
					有关更多详细信息，请查看我们的 <a href="../guides/composition">组合</a> 指南。
				</>
			),
		},
		{
			name: "forceMount",
			type: "boolean",
			description: (
				<span>
					当需要更多控制时，强制挂载。用于与 React 动画库控制动画时。
				</span>
			),
		},
		{
			name: "orientation",
			required: false,
			type: '"horizontal" | "vertical"',
			typeSimple: "enum",
			default: "vertical",
			description: "滚动条的方向",
		},
	]}
/>

<DataAttributesTable
	data={[
		{
			attribute: "[data-state]",
			values: ["visible", "hidden"],
		},
		{
			attribute: "[data-orientation]",
			values: ["vertical", "horizontal"],
		},
	]}
/>

### 拇指

在 `ScrollArea.Scrollbar` 中使用的拇指。

<PropsTable
	data={[
		{
			name: "asChild",
			required: false,
			type: "boolean",
			default: "false",
			description: (
				<>
					将默认渲染的元素更改为作为子元素传递的元素，合并它们的属性和行为。
					<br />
					<br />
					有关更多详细信息，请查看我们的 <a href="../guides/composition">组合</a> 指南。
				</>
			),
		},
	]}
/>

<DataAttributesTable
	data={[
		{
			attribute: "[data-state]",
			values: ["visible", "hidden"],
		},
	]}
/>

### 角落

垂直和水平滚动条交汇的角落。

<PropsTable
	data={[
		{
			name: "asChild",
			required: false,
			type: "boolean",
			default: "false",
			description: (
				<>
					将默认渲染的元素更改为作为子元素传递的元素，合并它们的属性和行为。
					<br />
					<br />
					有关更多详细信息，请查看我们的 <a href="../guides/composition">组合</a> 指南。
				</>
			),
		},
	]}
/>

## 可访问性

在大多数情况下，最好依靠原生滚动并使用 CSS 中的自定义选项。当这些不足够时，`ScrollArea` 提供了额外的可定制性，同时保持浏览器的原生滚动行为（以及可访问性功能，如键盘滚动）。

### 键盘交互

通过键盘滚动默认受到支持，因为该组件依赖于原生滚动。特定的键盘交互可能因平台而异，因此我们在这里不具体说明它们或添加特定的事件监听器来处理键盘事件的滚动。