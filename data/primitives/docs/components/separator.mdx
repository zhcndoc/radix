---
metaTitle: 分隔符
metaDescription: 视觉或语义上分隔内容。
name: separator
aria: https://www.w3.org/TR/wai-aria-1.2/#separator
---

# 分隔符

<Description>视觉或语义上分隔内容。</Description>

<HeroContainer>
	<SeparatorDemo />
</HeroContainer>

<HeroCodeBlock folder="Separator" />

<Highlights features={["支持水平和垂直方向。"]} />

## 安装

从命令行安装该组件。

```bash
npm install @radix-ui/react-separator
```

## 解剖结构

导入所有部分并将它们组合在一起。

```jsx
import { Separator } from "radix-ui";

export default () => <Separator.Root />;
```

## API 参考

### 根元素

分隔符。

<PropsTable
	data={[
		{
			name: "asChild",
			required: false,
			type: "boolean",
			default: "false",
			description: (
				<>
					将默认渲染的元素更改为作为子元素传递的元素，
					合并它们的属性和行为。
					<br />
					<br />
					请阅读我们的 <a href="../guides/composition">组合</a> 指南以获取
					更多详细信息。
				</>
			),
		},
		{
			name: "orientation",
			type: '"horizontal" | "vertical"',
			typeSimple: "枚举",
			default: '"horizontal"',
			description: "分隔符的方向。",
		},
		{
			name: "decorative",
			type: "boolean",
			description: (
				<span>
					当 <Code>true</Code> 时，表示它是纯视觉的，不具有
					语义含义，并确保它不出现在可访问性
					树中。
				</span>
			),
		},
	]}
/>

<DataAttributesTable
	data={[
		{
			attribute: "[data-orientation]",
			values: ["vertical", "horizontal"],
		},
	]}
/>

## 可访问性

遵循 [`separator` 角色要求](https://www.w3.org/TR/wai-aria-1.2/#separator)。