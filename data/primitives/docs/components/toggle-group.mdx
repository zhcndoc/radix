---
metaTitle: 切换组
metaDescription: 一组可以切换开启或关闭的两态按钮。
name: toggle-group
aria: https://www.w3.org/WAI/ARIA/apg/patterns/button
---

# 切换组

<Description>
	一组可以切换开启或关闭的两态按钮。
</Description>

<HeroContainer>
	<ToggleGroupDemo />
</HeroContainer>

<HeroCodeBlock folder="ToggleGroup" />

<Highlights
	features={[
		"完整的键盘导航。",
		"支持水平/垂直方向。",
		"支持单个和多个被按下的按钮。",
		"可以是受控或非受控。",
	]}
/>

## 安装

从命令行安装组件。

```bash
npm install @radix-ui/react-toggle-group
```

## 解剖学

导入组件。

```jsx
import { ToggleGroup } from "radix-ui";

export default () => (
	<ToggleGroup.Root>
		<ToggleGroup.Item />
	</ToggleGroup.Root>
);
```

## API 参考

### 根

包含切换组的所有部分。

<PropsTable
	data={[
		{
			name: "asChild",
			required: false,
			type: "boolean",
			default: "false",
			description: (
				<>
					更改默认渲染的元素为作为子元素传递的元素，
					合并其属性和行为。
					<br />
					<br />
					阅读我们的 <a href="../guides/composition">组合</a> 指南以获取
					更多详细信息。
				</>
			),
		},
		{
			name: "type",
			required: true,
			type: '"single" | "multiple"',
			typeSimple: "enum",
			description: (
				<span>
					确定一次可以被按下的项目是单个还是多个。
				</span>
			),
		},
		{
			name: "value",
			required: false,
			type: "string",
			description: (
				<span>
					当 <Code>type</Code> 为 <Code>"single"</Code> 时被按下的项目的受控值。必须与{" "}
					<Code>onValueChange</Code> 一起使用。
				</span>
			),
		},
		{
			name: "defaultValue",
			required: false,
			type: "string",
			description: (
				<span>
					当最初渲染并且 <Code>type</Code> 为 <Code>"single"</Code> 时显示为按下的项目的值。使用当您不需要控制项目的状态时。
				</span>
			),
		},
		{
			name: "onValueChange",
			required: false,
			type: "(value: string) => void",
			typeSimple: "function",
			description: (
				<span>
					在项目的按下状态更改时被调用的事件处理程序，当 <Code>type</Code> 为 <Code>"single"</Code>。
				</span>
			),
		},
		{
			name: "value",
			required: false,
			default: "[]",
			type: "string[]",
			description: (
				<span>
					当 <Code>type</Code> 为 <Code>"multiple"</Code> 时被按下的项目的受控值。必须与{" "}
					<Code>onValueChange</Code> 一起使用。
				</span>
			),
		},
		{
			name: "defaultValue",
			required: false,
			default: "[]",
			type: "string[]",
			description: (
				<span>
					当最初渲染并且 <Code>type</Code> 为 <Code>"multiple"</Code> 时显示为按下的项目的值。使用当您不需要控制项目的状态时。
				</span>
			),
		},
		{
			name: "onValueChange",
			required: false,
			type: "(value: string[]) => void",
			typeSimple: "function",
			description: (
				<span>
					在项目的按下状态更改时被调用的事件处理程序，当 <Code>type</Code> 为 <Code>"multiple"</Code>。
				</span>
			),
		},
		{
			name: "disabled",
			required: false,
			type: "boolean",
			default: "false",
			description: (
				<span>
					当 <Code>true</Code> 时，防止用户与切换组及其所有项目交互。
				</span>
			),
		},
		{
			name: "rovingFocus",
			required: false,
			type: "boolean",
			default: "true",
			description: (
				<span>
					当 <Code>false</Code> 时，使用箭头键导航项目将被禁用。
				</span>
			),
		},
		{
			name: "orientation",
			required: false,
			type: '"horizontal" | "vertical" | undefined',
			typeSimple: "enum",
			default: "undefined",
			description: (
				<span>
					组件的方向，决定焦点如何移动：{" "}
					<Code>horizontal</Code> 用于左右箭头，{" "}
					<Code>vertical</Code> 用于上下箭头。
				</span>
			),
		},
		{
			name: "dir",
			required: false,
			type: '"ltr" | "rtl"',
			typeSimple: "enum",
			description: (
				<span>
					切换组的阅读方向。如果省略，则全局继承自 <Code>DirectionProvider</Code> 或默认假设为 LTR
					(从左到右) 阅读模式。
				</span>
			),
		},
		{
			name: "loop",
			required: false,
			type: "boolean",
			default: "true",
			description: (
				<span>
					当 <Code>true</Code> 且 <Code>rovingFocus</Code> 为{" "}
					<Code>true</Code> 时，键盘导航将从最后一个项目循环到第一个项目，反之亦然。
				</span>
			),
		},
	]}
/>

<DataAttributesTable
	data={[
		{
			attribute: "[data-orientation]",
			values: ["vertical", "horizontal"],
		},
	]}
/>

### 项目

组中的一个项目。

<PropsTable
	data={[
		{
			name: "asChild",
			required: false,
			type: "boolean",
			default: "false",
			description: (
				<>
					更改默认渲染的元素为作为子元素传递的元素，
					合并其属性和行为。
					<br />
					<br />
					阅读我们的 <a href="../guides/composition">组合</a> 指南以获取
					更多详细信息。
				</>
			),
		},
		{
			name: "value",
			required: true,
			type: "string",
			description: "项目的唯一值。",
		},
		{
			name: "disabled",
			type: "boolean",
			description: (
				<span>
					当 <Code>true</Code> 时，防止用户与该项目交互。
				</span>
			),
		},
	]}
/>

<DataAttributesTable
	data={[
		{
			attribute: "[data-state]",
			values: ["on", "off"],
		},
		{
			attribute: "[data-disabled]",
			values: "禁用时存在",
		},
		{
			attribute: "[data-orientation]",
			values: ["vertical", "horizontal"],
		},
	]}
/>

## 示例

### 确保始终有值

您可以控制组件以确保一个值。

```jsx line=5,8
import * as React from "react";
import { ToggleGroup } from "radix-ui";

export default () => {
	const [value, setValue] = React.useState("left");

	return (
		<ToggleGroup.Root
			type="single"
			value={value}
			onValueChange={(value) => {
				if (value) setValue(value);
			}}
		>
			<ToggleGroup.Item value="left">
				<TextAlignLeftIcon />
			</ToggleGroup.Item>
			<ToggleGroup.Item value="center">
				<TextAlignCenterIcon />
			</ToggleGroup.Item>
			<ToggleGroup.Item value="right">
				<TextAlignRightIcon />
			</ToggleGroup.Item>
		</ToggleGroup.Root>
	);
};
```

## 可访问性

使用 [移动 tabindex](https://www.w3.org/TR/wai-aria-practices-1.2/examples/radio/radio.html) 来管理项目之间的焦点移动。

### 键盘交互

<KeyboardTable
	data={[
		{
			keys: ["Tab"],
			description:
				"将焦点移动到被按下的项目或组中的第一个项目。",
		},
		{
			keys: ["Space"],
			description: "激活/取消激活该项目。",
		},
		{
			keys: ["Enter"],
			description: "激活/取消激活该项目。",
		},
		{
			keys: ["ArrowDown"],
			description: "将焦点移动到组中的下一个项目。",
		},
		{
			keys: ["ArrowRight"],
			description: "将焦点移动到组中的下一个项目。",
		},
		{
			keys: ["ArrowUp"],
			description: "将焦点移动到组中的上一个项目。",
		},
		{
			keys: ["ArrowLeft"],
			description: "将焦点移动到组中的上一个项目。",
		},
		{
			keys: ["Home"],
			description: "将焦点移动到第一个项目。",
		},
		{
			keys: ["End"],
			description: "将焦点移动到最后一个项目。",
		},
	]}
/>