---
metaTitle: 切换
metaDescription: 可以是开或关的双状态按钮。
name: toggle
aria: https://www.w3.org/WAI/ARIA/apg/patterns/button
---

# 切换

<Description>可以是开或关的双状态按钮。</Description>

<HeroContainer>
	<ToggleDemo />
</HeroContainer>

<HeroCodeBlock folder="Toggle" />

<Highlights
	features={["完全支持键盘导航。", "可以是受控或非受控。"]}
/>

## 安装

从命令行安装组件。

```bash
npm install @radix-ui/react-toggle
```

## 结构

导入组件。

```jsx
import { Toggle } from "radix-ui";

export default () => <Toggle.Root />;
```

## API 参考

### 根节点

切换。

<PropsTable
	data={[
		{
			name: "asChild",
			required: false,
			type: "boolean",
			default: "false",
			description: (
				<>
					改变默认渲染元素为传递的子元素，合并它们的属性和行为。
					<br />
					<br />
					阅读我们的 <a href="../guides/composition">组合</a> 指南以获取更多详细信息。
				</>
			),
		},
		{
			name: "defaultPressed",
			type: "boolean",
			description:
				"切换初次渲染时的按下状态。使用它当你不需要控制其按下状态时。",
		},
		{
			name: "pressed",
			type: "boolean",
			description: (
				<span>
					切换的受控按下状态。必须与 <Code>onPressedChange</Code> 一起使用。
				</span>
			),
		},
		{
			name: "onPressedChange",
			type: "(pressed: boolean) => void",
			typeSimple: "function",
			description:
				"当切换的按下状态发生变化时调用的事件处理程序。",
		},
		{
			name: "disabled",
			type: "boolean",
			description: (
				<span>
					当 <Code>true</Code> 时，阻止用户与切换进行交互。
				</span>
			),
		},
	]}
/>

<DataAttributesTable
	data={[
		{
			attribute: "[data-state]",
			values: ["on", "off"],
		},
		{
			attribute: "[data-disabled]",
			values: "在禁用时存在",
		},
	]}
/>

## 可访问性

### 键盘互动

<KeyboardTable
	data={[
		{
			keys: ["Space"],
			description: "激活/停用切换。",
		},
		{
			keys: ["Enter"],
			description: "激活/停用切换。",
		},
	]}
/>