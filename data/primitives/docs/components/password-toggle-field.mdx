---
metaTitle: 密码切换字段
metaDescription: 一个带有集成按钮以切换值可见性的密码输入字段
name: 密码切换字段
---

# 密码切换字段

<Description>
    一个带有集成按钮以切换值的可见性的密码输入字段。
</Description>

<HeroContainer>
    <PasswordToggleFieldDemo />
</HeroContainer>

<HeroCodeBlock folder="PasswordToggleField" />

<Highlights
    features={[
        "当通过指针切换时，将焦点返回到输入框",
        "当通过键盘或虚拟导航切换时保持按钮焦点",
        "表单提交后将可见性重置为隐藏，以防意外存储",
        "隐式可访问性标签用于基于图标的切换按钮",
    ]}
/>

## 解剖结构

导入所有部分并将它们组合在一起。

```jsx
import { unstable_PasswordToggleField as PasswordToggleField } from "radix-ui";
import { EyeClosedIcon, EyeOpenIcon } from "@radix-ui/react-icons";

export default () => (
    <PasswordToggleField.Root>
        <PasswordToggleField.Input />
        <PasswordToggleField.Toggle>
            <PasswordToggleField.Icon
                visible={<EyeOpenIcon />}
                hidden={<EyeClosedIcon />}
            />
        </PasswordToggleField.Toggle>
    </PasswordToggleField.Root>
);
```

## API 参考

### Root

包含密码切换字段的所有部分。

<PropsTable
    data={[
        {
            name: "id",
            required: false,
            type: "string",
            description: (
                <span>
                    字段的 ID。此 ID 用作用于可访问性属性的嵌套 DOM 元素 ID 的基础。
                </span>
            ),
        },
        {
            name: "visible",
            required: false,
            type: "boolean",
            description: (
                <span>
                    字段的受控可见性状态。必须与 <Code>onVisiblityChange</Code> 一起使用。
                </span>
            ),
        },
        {
            name: "defaultVisible",
            required: false,
            type: "boolean",
            description: (
                <span>
                    初始渲染时字段的可见性状态。当您不需要控制可见性状态时使用。
                </span>
            ),
        },
        {
            name: "onVisiblityChange",
            required: false,
            type: "(visible: boolean) => void",
            typeSimple: "function",
            description: (
                <span>
                    当字段的可见性状态发生变化时调用的事件处理程序。
                </span>
            ),
        },
    ]}
/>

### Input

渲染包含密码值的输入框。

<PropsTable
    data={[
        {
            name: "asChild",
            required: false,
            type: "boolean",
            default: "false",
            description: (
                <>
                    将默认渲染的元素更改为作为子元素传递的元素，合并它们的属性和行为。
                    <br />
                    <br />
                    查看我们的 <a href="../guides/composition">组合</a> 指南以获取更多细节。
                </>
            ),
        },
        {
            name: "autoComplete",
            required: false,
            type: '"current-password" | "new-password" | "off"',
            typeSimple: "enum",
            default: '"current-password"',
            description: (
                <span>
                    指定用户代理在填写表单字段值时是否可以提供自动化帮助，以及为浏览器提供有关字段中预计的信息类型的指导。
                </span>
            ),
        },
    ]}
/>

### Toggle

<PropsTable
    data={[
        {
            name: "asChild",
            required: false,
            type: "boolean",
            default: "false",
            description: (
                <>
                    将默认渲染的元素更改为作为子元素传递的元素，合并它们的属性和行为。
                    <br />
                    <br />
                    查看我们的 <a href="../guides/composition">组合</a> 指南以获取更多细节。
                </>
            ),
        },
    ]}
/>

### Slot

<PropsTable
    data={[
        {
            name: "render",
            required: false,
            type: "(props: { visible: boolean }) => void",
            typeSimple: "function",
            description: (
                <span>
                    返回要渲染的 React 节点的函数。可以用于对渲染进行更精确的控制。
                </span>
            ),
        },
        {
            name: "visible",
            required: false,
            type: "boolean",
            description: (
                <span>
                    当字段的可见性状态是可见时要渲染的 React 节点。
                </span>
            ),
        },
        {
            name: "hidden",
            required: false,
            type: "boolean",
            description: (
                <span>
                    当字段的可见性状态是隐藏时要渲染的 React 节点。
                </span>
            ),
        },
    ]}
/>

### Icon

<PropsTable
    data={[
        {
            name: "asChild",
            required: false,
            type: "boolean",
            default: "false",
            description: (
                <>
                    将默认渲染的元素更改为作为子元素传递的元素，合并它们的属性和行为。
                    <br />
                    <br />
                    查看我们的 <a href="../guides/composition">组合</a> 指南以获取更多细节。
                </>
            ),
        },
        {
            name: "visible",
            required: true,
            type: "boolean",
            description: (
                <span>
                    当字段的可见性状态是可见时要渲染的 SVG 图标。
                </span>
            ),
        },
        {
            name: "hidden",
            required: true,
            type: "boolean",
            description: (
                <span>
                    当字段的可见性状态是隐藏时要渲染的 SVG 图标。
                </span>
            ),
        },
    ]}
/>

## 示例

### 基本用法

```jsx
<PasswordToggleField.Root>
    <PasswordToggleField.Input />
    <PasswordToggleField.Toggle>
        <PasswordToggleField.Icon
            visible={<EyeOpenIcon />}
            hidden={<EyeClosedIcon />}
        />
    </PasswordToggleField.Toggle>
</PasswordToggleField.Root>
```

### 使用 `Slot`

```jsx
<PasswordToggleField.Root>
    <PasswordToggleField.Input />
    <PasswordToggleField.Toggle>
        <PasswordToggleField.Slot visible="🙊" hidden="🙈" />
    </PasswordToggleField.Toggle>
</PasswordToggleField.Root>
```

### 使用 `Slot` + `render` 属性

```jsx
<PasswordToggleField.Root>
    <PasswordToggleField.Input />
    <PasswordToggleField.Toggle>
        <PasswordToggleField.Slot
            render={({ visible }) => (
                <svg aria-hidden viewBox="0 0 2 2" xmlns="http://www.w3.org/2000/svg">
                    <path d={visible ? "M1 1 L2 2" : "M2 1 L1 2"} />
                </svg>
            )}
        />
    </PasswordToggleField.Toggle>
</PasswordToggleField.Root>
```